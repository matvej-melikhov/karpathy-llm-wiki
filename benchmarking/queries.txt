# Тестовый набор query для замера § 4.4.2.
# Одна строка = один query. Пустые строки и комментарии (#) игнорируются.
# Запросы отобраны так, чтобы покрыть разные домены и разную «глубину»:
# concept (PPO, Reward Hacking), сравнение (LambdaRank vs LambdaMART),
# процесс (как RLHF), общее (как устроен бустинг).

что такое PPO
как RLHF использует Bradley-Terry
в чём разница LambdaRank и LambdaMART
как устроен бустинг
что такое Reward Hacking
