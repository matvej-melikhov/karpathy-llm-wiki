---
name: snapshot
description: >
  Слепок vault: метрики связности и семантики + два визуальных ракурса
  (UMAP-карта по эмбеддингам и force-directed граф по wikilinks). Wrapper
  над `bin/knowledge_map.py`. Создаёт три версионированных артефакта:
  `_attachments/snapshot-YYYY-MM-DD.html` (UMAP),
  `_attachments/snapshot-graph-YYYY-MM-DD.html` (граф) и markdown-сводку
  в `wiki/meta/snapshots/snapshot-YYYY-MM-DD.md`. Используется для замера
  здоровья базы и для иллюстрации эволюции vault. Триггеры: /snapshot,
  snapshot, "слепок vault", "слепок базы", "снимок vault", "сделай слепок",
  "обнови слепок", "карта знаний", "knowledge map", "umap wiki",
  "визуализируй wiki".
---

# snapshot: слепок vault на дату

Скилл — тонкая обёртка над `bin/knowledge_map.py`. Сам Python-скрипт делает всю работу:
читает `wiki/meta/embeddings.json`, проецирует 4096-мерные эмбеддинги в 2D через UMAP,
рендерит обе карты через Cytoscape.js (`bin/wiki_graph.py`), считает Louvain
сообщества + bridge nodes + sparse-метрики, собирает markdown-обёртку.

Два HTML — **не дубликат**, а разные ракурсы:

- `snapshot-*.html` (Cytoscape, layout=preset, координаты — UMAP):
  близость на экране = **семантическая** близость (по эмбеддингам). Compound-облака
  отключены — Louvain-кластеры на семантической карте искажают чтение.
- `snapshot-graph-*.html` (Cytoscape, layout=fcose): близость на экране = **топологическая**
  связность (плотность wikilinks). Сообщества Louvain — compound nodes (полупрозрачные
  облака), узлы-мосты — звезда с золотым свечением.

В обеих картах: hover на узле подсвечивает его 1-hop соседей и затемняет остальное.

Расхождения между двумя картами и есть самое интересное (страница семантически близка
к одному кластеру, но цитируется в другом — кандидат для cross-link или re-domain).

Скилл нужен только для: (1) маршрутизации запроса к скрипту, (2) подачи результата пользователю.

**Свежесть эмбеддингов — не забота скилла.** `wiki/meta/embeddings.json` поддерживается актуальным через Stop-hook (`python3 bin/embed.py update` после каждого turn'а агента). Скилл просто потребляет последний снимок.

---

## Команда и флаги

| Команда | Поведение |
|---|---|
| `/snapshot` | Полная генерация: оба HTML + markdown-сводка. Дефолтный `--seed 42`. |
| `/snapshot --no-edges` | Без wikilink-граней на UMAP-карте (если перегружено). Cytoscape-граф не затрагивается. |
| `/snapshot --no-graph` | Пропустить Cytoscape-граф (только UMAP + markdown). |
| `/snapshot --no-page` | Только HTML-артефакты, без markdown в `wiki/meta/snapshots/`. Для одноразовой проверки. |
| `/snapshot --seed N` | Другой UMAP random_state — даёт другую укладку точек (используй, если дефолтная плохо разделяет кластеры). |

Под капотом — `python3 bin/knowledge_map.py [флаги]`. Плюс `--out-dir <DIR>` если нужен другой каталог для HTML (по умолчанию `_attachments/`).

---

## Поток

```
1. python3 bin/knowledge_map.py [флаги]   # генерация
2. Парсинг stdout: пути к артефактам + сводная статистика
3. Сообщение пользователю — пути обоими wikilink'ами + ключевые цифры
```

Если `wiki/meta/embeddings.json` отсутствует — скрипт упадёт. Это означает, что Stop-hook ещё ни разу не успешно отработал (например, embedder недоступен). Сообщи пользователю — это разовая проблема окружения, не задача скилла.

---

## Output для пользователя

После успешного прогона — короткое сообщение:

```
🗺️ Слепок готов.

- UMAP (семантика): [[_attachments/snapshot-YYYY-MM-DD.html]]
- Граф (топология): [[_attachments/snapshot-graph-YYYY-MM-DD.html]]
- Markdown-сводка: [[wiki/meta/snapshots/snapshot-YYYY-MM-DD]]

Метрики: <страниц>, <доменов>, <wikilink-граней>, <сирот>, Q=<Q>.
```

Если генерация графа была пропущена (`--no-graph` или Cytoscape-зависимости отсутствуют), не упоминай его в выводе. Если в stdout есть строки с warnings (например, страницы без эмбеддинга, fallback на серый цвет) — упомяни их.

---

## Формат markdown-сводки

Markdown-страница генерируется скриптом — скилл её не правит. Логика:

1. **Главные метрики** одной таблицей сверху — то, ради чего обычно открывают slepok. Сюда попадают: страниц всего, доменов, wikilinks, сирот, Q-модулярность, сообществ, медиана попарной близости, 95-й перцентиль, топ-мост, самая связанная, самая близкая пара.
2. **Карты** (две iframe-обёртки) сразу под метриками.
3. **Подробные таблицы** (по доменам, сообществам, мостам, близостям, разреженным сообществам, связности доменов) — в `<details>` блоках, чтобы не загромождать первый экран.
4. **Пояснения** («Как читать», «Что такое Q», «Что такое participation») — в финальном `<details>` блоке.

Числа на первом экране; пояснения и длинные таблицы — за разворотом.

---

## Что скилл не делает

- **Не редактирует content-файлы** (`wiki/ideas/`, `wiki/entities/`). Артефакты пишутся только в `_attachments/` и `wiki/meta/snapshots/`.
- **Не учитывается в `wiki_hash` lint'а** — папка `wiki/meta/snapshots/` исключена в `bin/static_lint.py` (см. lint SKILL, секция Skip-check). Поэтому `/snapshot` не триггерит fresh-audit при следующем `/lint`.
- **Не запускает embedder.** Свежесть `wiki/meta/embeddings.json` — забота Stop-hook'а в `.claude/settings.json`. Если эмбеддингов нет, скрипт падает — пользователь сам разбирается с окружением.
- **Не удаляет старые snapshot'ы.** История `snapshot-*.md` в `snapshots/` накапливается специально — это диахронический срез эволюции wiki (как `lint-report-*.md`).

---

## Когда вызывать проактивно

После крупного ingest (≥5 новых страниц) или batch-ingest — упомянуть в финальном отчёте: «можно обновить слепок через `/snapshot`». Не запускать без явного запроса — генерация занимает 10-30 секунд и тратит память на UMAP.

Для иллюстраций ВКР — слепок это самый зрелищный артефакт, у пользователя могут быть запросы вида «сделай свежий снимок до защиты».
